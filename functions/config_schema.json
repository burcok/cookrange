{
  "_meta": {
    "description": "Canonical, hand-edited source of truth for every admin-editable application setting. Add or retune a setting HERE ONLY — Dart defaults/schema (lib/core/config/*.g.dart), the TypeScript admin-panel schema (config-schema.ts), and the JS runtime reader (functions/app_config.js) are all derived from this file by scripts/gen_config_schema.mjs. CI fails if generated files are stale (git diff --exit-code after regenerating).",
    "schema_version": 1,
    "fields": {
      "type": "int | double | bool | string | enum | string_list | map<K,V> — the map's value shape is documented per-entry in `note`",
      "default": "MUST equal the current hardcoded value at migration time (Faz 0). This is what every reader falls back to if the config doc is missing/unreachable — it is the fail-safe, not just a placeholder.",
      "min / max / enum": "Enforced by the updateAppConfig callable at write time AND by the read-side clamp in both runtimes (defense in depth). Where a hard ceiling already exists in code today (e.g. ai.max_tokens's Math.min(...,32000) in functions/index.js), min/max reproduce it exactly rather than inventing a new bound.",
      "doc": "critical | client | server — which app_config/{doc} this setting lives in. Placement rule: a setting lives in the most restrictive document that still contains every one of its readers. See PLAN.md Faz A §A2.",
      "group": "UI section grouping in the admin panel's config editor (not a Firestore path).",
      "label": "Human-readable admin-panel label (Turkish, per K6).",
      "sensitive": "true = the updateAppConfig callable requires confirm:true + a reason string (>=10 chars) to write this field. Applied to: all economic values, all ecosystem kill-switches, maintenance.enabled, version.force_update, and anti-abuse thresholds whose weakening has real abuse consequences.",
      "note": "file:line the current hardcoded value came from — this is what makes eventual deletion of the constant (Faz A Faz 4) auditable, and documents any related/mirrored constant or hidden code-level clamp this entry must stay consistent with."
    }
  },

  "maintenance.enabled": { "type": "bool", "default": false, "doc": "critical", "group": "maintenance", "label": "Bakım modu", "sensitive": true, "note": "app_config_model.dart MaintenanceConfig.enabled default false; route_guard.dart:125 reads it. Fail direction: sticky-closed once observed true (see A4) — this is a deliberate EXCEPTION to 'fail to code default', not a bug." },
  "maintenance.message": { "type": "map<string,string>", "default": {}, "doc": "critical", "group": "maintenance", "label": "Bakım mesajı (en/tr)", "sensitive": false, "note": "app_config_model.dart MaintenanceConfig.message (LocalizedText); maintenance_screen.dart:43 reads it." },

  "version.min_supported_android": { "type": "string", "default": "0.0.0", "doc": "critical", "group": "version", "label": "Android minimum desteklenen sürüm", "sensitive": false, "note": "app_config_model.dart VersionConfig.minSupportedAndroid; version_gate.dart:37." },
  "version.min_supported_ios": { "type": "string", "default": "0.0.0", "doc": "critical", "group": "version", "label": "iOS minimum desteklenen sürüm", "sensitive": false, "note": "version_gate.dart:38. Cross-field: must be a valid semver and SHOULD be <= version.latest_ios (checked at write time, not enforced as a hard rule since a same-version min==latest is legitimate)." },
  "version.latest_android": { "type": "string", "default": "0.0.0", "doc": "critical", "group": "version", "label": "Android en güncel sürüm", "sensitive": false, "note": "version_gate.dart:44 — feeds isUpdateAvailable(), currently DEAD (zero callers). Kept in schema since the field is real and admin-editable today; not a candidate for deletion by this migration." },
  "version.latest_ios": { "type": "string", "default": "0.0.0", "doc": "critical", "group": "version", "label": "iOS en güncel sürüm", "sensitive": false, "note": "version_gate.dart:45." },
  "version.force_update": { "type": "bool", "default": false, "doc": "critical", "group": "version", "label": "Zorunlu güncelleme", "sensitive": true, "note": "version_gate.dart:59. Fail-open (missing config never bricks the app) — route_guard.dart:128 already guards with `_currentVersion != null &&`." },
  "version.update_message": { "type": "map<string,string>", "default": {}, "doc": "critical", "group": "version", "label": "Güncelleme mesajı (en/tr)", "sensitive": false, "note": "force_update_screen.dart — currently editable in admin UI but DEAD (never rendered). Kept as-is; not this migration's job to fix that gap, only to preserve current behavior." },
  "version.android_store_url": { "type": "string", "default": "", "doc": "critical", "group": "version", "label": "Play Store URL", "sensitive": false, "note": "force_update_screen.dart:21." },
  "version.ios_store_url": { "type": "string", "default": "", "doc": "critical", "group": "version", "label": "App Store URL", "sensitive": false, "note": "force_update_screen.dart:21." },

  "announcement.enabled": { "type": "bool", "default": false, "doc": "critical", "group": "announcement", "label": "Duyuru aktif", "sensitive": false, "note": "announcement_banner.dart." },
  "announcement.id": { "type": "string", "default": "", "doc": "critical", "group": "announcement", "label": "Duyuru ID (dismiss tracking)", "sensitive": false, "note": "announcement_banner.dart — used as the per-device dismissal key." },
  "announcement.message": { "type": "map<string,string>", "default": {}, "doc": "critical", "group": "announcement", "label": "Duyuru mesajı (en/tr)", "sensitive": false, "note": "announcement_banner.dart." },
  "announcement.type": { "type": "enum", "enum": ["info", "warning", "success", "promo"], "default": "info", "doc": "critical", "group": "announcement", "label": "Duyuru tipi", "sensitive": false, "note": "announcement_banner.dart — enum values inferred from banner style variants; verify exact set against the widget before Faz 2 write-path ships." },
  "announcement.cta_url": { "type": "string", "default": "", "doc": "critical", "group": "announcement", "label": "Duyuru CTA URL", "sensitive": false, "note": "announcement_banner.dart." },
  "announcement.dismissible": { "type": "bool", "default": true, "doc": "critical", "group": "announcement", "label": "Kapatılabilir", "sensitive": false, "note": "announcement_banner.dart." },

  "features.gym": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Spor salonu ekosistemi", "sensitive": true, "note": "feature_flags.dart:31. K5: kept visible/true — ADR-012's kill-switch now actually works (fail-open bug fixed); the value true is a conscious decision, not the old accidental default. Fail direction: schema default per-key, NEVER a blanket literal (A4) — this is the field that was silently always-on before this migration." },
  "features.coach": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Koç ekosistemi", "sensitive": true, "note": "feature_flags.dart:32. Same K5 reasoning as features.gym." },
  "features.programs": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Programlar (pazar yeri)", "sensitive": true, "note": "feature_flags.dart:33." },
  "features.squad": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Squad", "sensitive": false, "note": "feature_flags.dart:34." },
  "features.meal_plan_templates": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Plan şablonları", "sensitive": false, "note": "feature_flags.dart:41 (mealPlanTemplates) — independent kill-switch from gym/coach so a template-generation problem doesn't require killing the whole dashboard." },
  "features.gym_invite_codes": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Salon davet kodları", "sensitive": false, "note": "feature_flags.dart:47 (gymInviteCodes)." },
  "features.gym_attribution": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Salon atfetme/komisyon UI", "sensitive": false, "note": "feature_flags.dart:56 (gymAttribution)." },
  "features.community": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Topluluk", "sensitive": false, "note": "admin_app_config_screen.dart _featureKeys — toggleable in admin UI today but ZERO FeatureFlags consumers currently read this key; kept as a real schema entry since the admin surface already exposes it and a future call site may consult it." },
  "features.chat": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Sohbet", "sensitive": false, "note": "admin_app_config_screen.dart _featureKeys — same zero-consumer caveat as features.community." },
  "features.food_scan": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Yemek fotoğrafı tarama", "sensitive": false, "note": "admin_app_config_screen.dart _featureKeys — same zero-consumer caveat." },
  "features.marketplace": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Pazar yeri (genel)", "sensitive": true, "note": "admin_app_config_screen.dart _featureKeys — same zero-consumer caveat; marked sensitive anyway since 'marketplace' overlaps the gym/coach ecosystem's blast radius even though no code reads it yet." },
  "features.referral": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Referans sistemi", "sensitive": false, "note": "admin_app_config_screen.dart _featureKeys — same zero-consumer caveat." },
  "features.voice_assistant": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Sesli asistan", "sensitive": false, "note": "NEW — migrated from Firebase Remote Config's feature_voice_assistant default true (remote_config_service.dart:14-31), read at main_scaffold.dart:171. Part of the RC consolidation (A9)." },
  "features.nutrition_analytics": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Beslenme analitiği", "sensitive": false, "note": "NEW — migrated from Firebase Remote Config's feature_nutrition_analytics, read at home.dart:1388. Part of the RC consolidation (A9)." },
  "features.photo_analysis": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Fotoğraf analizi (AI)", "sensitive": false, "note": "REPLACES ai.photo_analysis_enabled (app_config_model.dart:73, editable in admin UI, zero readers). Deliberately folded into the generic features.* map instead of a second AI-specific boolean mechanism — one enable/disable mechanism, not two." },
  "features.weekly_recap": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Haftalık özet", "sensitive": false, "note": "REPLACES ai.weekly_recap_enabled (app_config_model.dart:74, zero readers). Same fold-into-features.* reasoning as features.photo_analysis." },
  "features.fitness_twin": { "type": "bool", "default": true, "doc": "critical", "group": "features", "label": "Fitness ikizi (AI)", "sensitive": false, "note": "REPLACES ai.fitness_twin_enabled (app_config_model.dart:75, not even in the admin UI today). Same fold-into-features.* reasoning." },

  "rollout.gym": { "type": "int", "default": 100, "min": 0, "max": 100, "doc": "critical", "group": "rollout", "label": "Salon — kademeli açılım %", "sensitive": false, "note": "app_config_model.dart AppConfig.rollout map — READ by isInRollout() (app_config_service.dart:124) but NEVER WRITTEN by admin_app_config_screen.dart's _buildPatch(). This is the single concrete bug this migration fixes for rollout: the admin UI regenerated from this schema (Faz 2) will finally write it. Default 100 = fully rolled out, matching today's de facto always-on state (pct==null → isInRollout returns true)." },
  "rollout.coach": { "type": "int", "default": 100, "min": 0, "max": 100, "doc": "critical", "group": "rollout", "label": "Koç — kademeli açılım %", "sensitive": false, "note": "Same as rollout.gym." },
  "rollout.programs": { "type": "int", "default": 100, "min": 0, "max": 100, "doc": "critical", "group": "rollout", "label": "Programlar — kademeli açılım %", "sensitive": false, "note": "Same as rollout.gym." },
  "rollout.squad": { "type": "int", "default": 100, "min": 0, "max": 100, "doc": "critical", "group": "rollout", "label": "Squad — kademeli açılım %", "sensitive": false, "note": "Same as rollout.gym." },

  "ai.text_model": { "type": "string", "default": "", "doc": "client", "group": "ai", "label": "Metin modeli (override)", "sensitive": false, "note": "app_config_model.dart AiConfig.textModel default '' — deliberately empty so a missing/blank config never overrides AIService._defaultModel='openai/gpt-4o-mini' (ai_service.dart:20). Read by BOTH client (app_config_service.dart:116, applyRemoteConfig) and server (index.js:372 model=modelByType[type]||aiCfg.text_model||DEFAULT_MODEL) — lives in `client` doc per the 'least restrictive doc containing every reader' rule." },
  "ai.vision_model": { "type": "string", "default": "", "doc": "client", "group": "ai", "label": "Görsel modeli (override)", "sensitive": false, "note": "AiConfig.visionModel default ''; ai_service.dart:29 _defaultVisionModel='meta-llama/llama-3.2-11b-vision-instruct:free'; server reads at index.js:369-371." },
  "ai.timeout_s": { "type": "int", "default": 90, "min": 10, "max": 300, "doc": "client", "group": "ai", "label": "AI istek zaman aşımı (sn)", "sensitive": false, "note": "AiConfig.timeoutS default 90; app_config_service.dart:118 applies to AIService._textTimeout (ai_service.dart:24, currently a hardcoded Duration(seconds:90))." },
  "ai.free_daily_limit": { "type": "int", "default": 2, "min": 0, "max": 100, "doc": "client", "group": "ai", "label": "Ücretsiz günlük AI hakkı", "sensitive": true, "note": "RESOLVED DRIFT (Faz 0): AiConfig.freeDailyLimit's OLD default was 5 (app_config_model.dart:71) but the server (index.js:31 FREE_DAILY_LIMIT) and AiCreditModel (ai_credit_model.dart:10) both already enforce/display 2. The server value wins because it's what's actually charged against — 2 is the correct default, not 5. Read by client (quota badge, ai_credits_sheet.dart:150) AND server (index.js:385, enforceRateLimitAndQuota)." },
  "ai.premium_daily_limit": { "type": "int", "default": 20, "min": 0, "max": 500, "doc": "client", "group": "ai", "label": "Premium günlük AI hakkı", "sensitive": true, "note": "RESOLVED DRIFT (Faz 0): OLD default 50 (app_config_model.dart:72) vs. server's actual 20 (index.js:32) and AiCreditModel's 20 (ai_credit_model.dart:11). Server value wins, same reasoning as ai.free_daily_limit. Cross-field: must be >= ai.free_daily_limit (enforced at write time, A5)." },
  "endpoints.ai_proxy_url": { "type": "string", "default": "", "doc": "client", "group": "ai", "label": "AI proxy URL (override)", "sensitive": true, "note": "AppConfig.aiProxyUrl default ''; app_config_service.dart:119 applies ONLY when non-empty, deliberately, so Firebase Remote Config's ai_proxy_url remains the fallback until this is populated in production. SEE A9's RC-removal warning: never remove Remote Config before this field is confirmed live in production on a real device — doing so falls back to calling OpenRouter directly with the on-device key (the exact exposure ADR-006 closed)." },

  "ai.model_by_type": { "type": "map<string,string>", "default": {}, "doc": "server", "group": "ai_server", "label": "Sorgu tipine göre model", "sensitive": true, "note": "AiConfig.modelByType default {}; ONLY the server reads this (index.js:366, summaries.js:352) — client's modelFor() has zero callers, so per the placement rule this belongs in `server`, not `client`, even though the Dart model class also parses it." },
  "ai.max_tokens": { "type": "int", "default": 8192, "min": 256, "max": 32000, "doc": "server", "group": "ai_server", "label": "Maksimum çıktı token (varsayılan)", "sensitive": false, "note": "VERIFIED against functions/index.js:375-379: `Math.min(Number(maxTokensByType[type] || aiCfg.max_tokens || MAX_OUTPUT_TOKENS) || MAX_OUTPUT_TOKENS, 32000)` — the max:32000 bound here is not invented, it reproduces an EXISTING hard ceiling already enforced in code today. MAX_OUTPUT_TOKENS=8192 (index.js:48) is itself the fallback used when this config value is absent, and it equals AiConfig.maxTokens's existing default (app_config_model.dart:65) — the two were already the same number, just declared twice." },
  "ai.max_tokens_by_type": { "type": "map<string,int>", "default": {}, "doc": "server", "group": "ai_server", "label": "Sorgu tipine göre maksimum token", "sensitive": false, "note": "AiConfig.maxTokensByType default {}; index.js:374. Each value should be clamped to the same 32000 ceiling as ai.max_tokens at write time." },
  "ai.temperature": { "type": "double", "default": 0.7, "min": 0.0, "max": 1.0, "doc": "server", "group": "ai_server", "label": "Model sıcaklığı", "sensitive": false, "note": "VERIFIED against index.js:381-384: `Math.min(Math.max(Number(body.temperature) || Number(aiCfg.temperature) || 0.7, 0), 1)` — min:0/max:1 reproduces this existing code-level clamp exactly, not a new bound." },
  "ai.max_retries": { "type": "int", "default": 3, "min": 0, "max": 10, "doc": "client", "group": "ai", "label": "AI çağrısı yeniden deneme sayısı", "sensitive": false, "note": "PLACEMENT FIX (Faz 4): originally authored as doc:server/group:ai_server, which — per the SAME placement rule ai.model_by_type's own note states (\"ONLY the server reads this... belongs in server, not client\") — was backwards here: ONLY AIService (Dart, client-side retry loop) ever reads this; no functions/*.js file references max_retries at all. A doc:server field is structurally unreadable by the client that actually needs it. Moved to doc:client/group:ai, alongside ai.timeout_s/ai.text_model. Now wired live via AppConfigService._set -> AIService.applyRemoteConfig (AiConfig.maxRetries, app_config_model.dart)." },
  "ai.retry_delay_s": { "type": "int", "default": 2, "min": 0, "max": 30, "doc": "client", "group": "ai", "label": "Yeniden deneme gecikmesi (sn)", "sensitive": false, "note": "PLACEMENT FIX (Faz 4): same misplacement and same fix as ai.max_retries directly above — client-only reader (AIService._retryDelay), moved from doc:server/group:ai_server to doc:client/group:ai. Now wired live via AppConfigService._set -> AIService.applyRemoteConfig (AiConfig.retryDelayS, app_config_model.dart)." },
  "ai.allowed_models": { "type": "string_list", "default": ["openrouter/free", "meta-llama/llama-3.2-11b-vision-instruct:free"], "doc": "server", "group": "ai_server", "label": "İzin verilen model listesi", "sensitive": true, "note": "AiConfig.allowedModels default [] — currently DEAD on both sides; the server's real allowlist is the hardcoded ALLOWED_MODELS Set (index.js:35-41), which ALSO includes two env-var-derived entries (process.env.OPENROUTER_VISION_MODEL, process.env.OPENROUTER_MODEL) that are deployment-specific and are NOT part of this default — they remain additive at runtime, orthogonal to admin config. The default-equality test (Faz 0) must assert this schema default is a SUBSET of the runtime ALLOWED_MODELS set, not full equality. Cross-field validation (A5): ai.text_model and every value in ai.model_by_type must be a member of this list whenever the list is non-empty — this is what finally makes ALLOWED_MODELS admin-configurable instead of requiring a redeploy to add a model." },
  "ai.model_pricing": { "type": "map<string,object>", "default": { "google/gemini-2.0-flash-001": { "in": 0.10, "out": 0.40 }, "openai/gpt-4o-mini": { "in": 0.15, "out": 0.60 }, "openai/gpt-4o": { "in": 2.50, "out": 10.0 } }, "doc": "server", "group": "ai_server", "label": "Model fiyatlandırması (USD/1M token)", "sensitive": true, "note": "MODEL_PRICING, functions/index.js:67-71, consumed by pricingFor() at :73. Values are USD per 1,000,000 tokens {in, out}. A model NOT in this map is priced {in:0,out:0,known:false} and flagged unpriced=true for the cost dashboard (per the metrics plan's F8). Server-only — exposing real per-token cost structure to any authenticated client is unnecessary." },
  "ai.max_messages": { "type": "int", "default": 30, "min": 1, "max": 200, "doc": "server", "group": "ai_server", "label": "Maksimum mesaj sayısı (sohbet)", "sensitive": false, "note": "MAX_MESSAGES, index.js:45." },
  "ai.max_total_chars": { "type": "int", "default": 24000, "min": 1000, "max": 200000, "doc": "server", "group": "ai_server", "label": "Maksimum toplam karakter (payload)", "sensitive": false, "note": "MAX_TOTAL_CHARS, index.js:46 — serialized messages payload size guard." },
  "ai.rate_window_ms": { "type": "int", "default": 60000, "min": 1000, "max": 3600000, "doc": "server", "group": "ai_server", "label": "AI hız sınırı penceresi (ms)", "sensitive": true, "note": "RATE_WINDOW_MS, index.js:51 — per-uid sliding window, independent of the daily quota." },
  "ai.rate_max_in_window": { "type": "int", "default": 12, "min": 1, "max": 1000, "doc": "server", "group": "ai_server", "label": "Pencere başına maksimum çağrı", "sensitive": true, "note": "RATE_MAX_IN_WINDOW, index.js:52." },

  "privacy.k_anonymity_threshold": { "type": "int", "default": 5, "min": 2, "max": 50, "doc": "client", "group": "privacy", "label": "K-anonimlik eşiği (salon analitiği)", "sensitive": true, "note": "RESOLVED: this is NOT the same setting as engagement_credit.js's WEEKLY_MIN_ACTIVE_MEMBERS=3 (that's an unrelated weekly-group-leaderboard threshold, see PLAN.md §4b). The ONLY real k-anonymity floor is GymAnalyticsModel.kAnonymityThreshold=5 (gym_analytics_model.dart:42), currently ENFORCED CLIENT-SIDE ONLY (gym_analytics_service.dart:145-153) — a separately-spawned fix (task_afc1b97e) is moving enforcement server-side; this config value should become the input to that server-side gate once built, not just the client's display-suppression threshold it is today. Default 5 preserved (raising the client's 5 rather than lowering to match the unrelated 3, since 5 is what docs/UI already promise)." },
  "gamification.level_curve_coefficient": { "type": "int", "default": 50, "min": 10, "max": 200, "doc": "client", "group": "gamification", "label": "Seviye eğrisi katsayısı", "sensitive": true, "note": "LEVEL_CURVE_COEFFICIENT, progress.js:182, mirrored by hand in xp_level_curve.dart:41 with a comment instructing a human to keep them in sync — this migration removes that hand-sync obligation entirely. RISK (see PLAN.md 'Bilinçli kabul edilen riskler' #2): editing this retroactively re-levels every user in the product. Tightly bounded (10-200) and marked sensitive (confirm+reason+±50%-force-guard) as the mitigation; this is arguably the one value that should have stayed in code, but K3 explicitly asked for full configurability including algorithmic tuning." },
  "gamification.max_level": { "type": "int", "default": 999, "min": 50, "max": 9999, "doc": "client", "group": "gamification", "label": "Maksimum seviye", "sensitive": false, "note": "MAX_LEVEL, progress.js:183, mirrored in xp_level_curve.dart:46. Defensive search bound, not a real gameplay ceiling (999 ≈ 49M XP, unreachable)." },

  "client.check_in_radius_m": { "type": "int", "default": 100, "min": 10, "max": 1000, "doc": "client", "group": "client_misc", "label": "Salon check-in yarıçapı (m)", "sensitive": false, "note": "GymModel.checkInRadius default 100 (gym_model.dart:120,155); also hardcoded as the initial state at gym_setup_screen.dart:98 and gym_checkin_screen.dart:24 (param default). Consumed by gym_presence_service.dart:75,122,232. Post-onboarding, authenticated-only usage → `client` doc is fine (no pre-auth read needed)." },
  "client.max_dishes_per_prompt": { "type": "int", "default": 180, "min": 20, "max": 500, "doc": "client", "group": "client_misc", "label": "Prompt başına maksimum yemek sayısı", "sensitive": false, "note": "PromptService.maxDishesPerPrompt, prompt_service.dart:17. Client-side prompt assembly (post-onboarding, authenticated) → `client` doc." },
  "client.weekly_meal_plan_expiry_days": { "type": "int", "default": 7, "min": 1, "max": 30, "doc": "client", "group": "client_misc", "label": "Haftalık plan geçerlilik süresi (gün)", "sensitive": false, "note": "weekly_meal_plan_service.dart:185,333, two call sites both Duration(days:7)." },

  "client.min_age_years": { "type": "int", "default": 16, "min": 13, "max": 21, "doc": "critical", "group": "compliance", "label": "Minimum yaş (KVKK/GDPR)", "sensitive": true, "note": "AgeGate.kMinimumAgeYears, age_gate.dart:10. PLACEMENT NOTE: this must be readable PRE-AUTH — per ADR-013, onboarding (which includes the age gate) runs entirely before account registration, so a `client` doc gated on isAuthenticated() would incorrectly block this read during onboarding. Placed in `critical` (public read) instead, despite being a compliance value, specifically for this pre-auth reason." },
  "client.referral_pending_code_ttl_days": { "type": "int", "default": 7, "min": 1, "max": 30, "doc": "critical", "group": "referral", "label": "Bekleyen referans kodu TTL (gün)", "sensitive": false, "note": "ReferralService.pendingCodeTtl, referral_service.dart:252. PLACEMENT NOTE: same pre-auth reasoning as client.min_age_years — DeepLinkService saves a pending /invite/{code} the moment it arrives with no signed-in session (surviving an app kill mid-onboarding), so this must be public-read to be usable during the onboarding referral step, not gated behind isAuthenticated()." },

  "economy.referral_reward_days": { "type": "int", "default": 7, "min": 1, "max": 90, "doc": "server", "group": "economy", "label": "Referans ödülü (gün)", "sensitive": true, "note": "REFERRAL_REWARD_DAYS, functions/economy.js:30 — the 7-day premium trial length, used at :179,:211,:215." },
  "economy.referral_max_uses": { "type": "int", "default": 10, "min": 1, "max": 1000, "doc": "server", "group": "economy", "label": "Referans kodu maksimum kullanım", "sensitive": false, "note": "REFERRAL_MAX_USES, economy.js:31. Also mirrored as a UI fallback default at admin_panel_screen.dart:1994 — that should eventually read this config value instead of its own hardcoded 10." },
  "economy.referral_commission_try": { "type": "double", "default": 5, "min": 0, "max": 1000, "doc": "server", "group": "economy", "label": "Referans komisyonu (₺, sabit)", "sensitive": true, "note": "REFERRAL_COMMISSION_TRY, economy.js:32 — flat TRY, written to the commission ledger at :198." },
  "economy.gym_commission_try": { "type": "map<string,double>", "default": { "com.cookrange.premium.monthly": 15, "com.cookrange.premium.yearly": 120 }, "doc": "server", "group": "economy", "label": "Salon komisyonu (₺, ürün başına)", "sensitive": true, "note": "GYM_COMMISSION_TRY, economy.js:44-47. Its own in-code comment: 'a placeholder rate pending the gym contract's business/legal sign-off ... change it here only' — migrating this to config is the literal fulfillment of that comment's intent. Keys are Play/App Store product IDs; read at economy.js:293 in maybeAwardGymCommission." },
  "economy.streak_freeze_grant_amount": { "type": "int", "default": 1, "min": 0, "max": 5, "doc": "server", "group": "economy", "label": "Yeni kullanıcı streak dondurma hediyesi", "sensitive": false, "note": "Hardcoded at lib/core/services/firestore_service.dart:162: 'streak_freeze_count': 1, // welcome gift for new users. Written ONLY at account creation; there is no refill/grant path anywhere else in the codebase, and firestore.rules bounds it to [0,1] at create time (SEC-14/29 this session). Migrating this to `server`-doc config means the create-time Cloud-Function-side write (not yet built as of this session's SEC work — verify current create path before wiring) reads it, NOT a client-writable field." },

  "purchases.products": { "type": "map<string,object>", "default": { "com.cookrange.premium.monthly": { "kind": "subscription", "tier": "premium", "days": 31 }, "com.cookrange.premium.yearly": { "kind": "subscription", "tier": "premium", "days": 365 }, "cookrange_ai_credits_10": { "kind": "consumable", "bonusCredits": 10 } }, "doc": "server", "group": "economy", "label": "Ürün kataloğu", "sensitive": true, "note": "PRODUCTS, functions/purchases.js:47-51 — 'Mirrors BillingProducts on the client' per its own comment; that Dart-side mirror is a separate, not-yet-migrated hardcoded catalog to reconcile in Faz 4. Adding/changing a store product becomes a config edit instead of a redeploy." },

  "gamification.xp_table": { "type": "map<string,object>", "default": { "meal_logged": { "points": 5, "dailyCap": 4 }, "streak_day": { "points": 10, "dailyCap": 1 }, "check_in": { "points": 15, "dailyCap": 1 }, "post_created": { "points": 20, "dailyCap": 2 }, "comment_created": { "points": 5, "dailyCap": 10 }, "reaction_given": { "points": 1, "dailyCap": 20 }, "recipe_cooked": { "points": 25, "dailyCap": 2 }, "template_accepted": { "points": 30, "dailyCap": null } }, "doc": "server", "group": "gamification_server", "label": "XP tablosu", "sensitive": true, "note": "XP_TABLE, functions/progress.js:97-106. MIGRATION HAZARD (see PLAN.md A7): this table is imported at MODULE SCOPE by presence.js:32 and templates.js:29 (`const { awardXp, XP_TABLE } = require('./progress')`) — making it config-backed requires those two import sites to receive the table as an argument resolved in the request path, NOT a module-scope import. This is the highest-risk single edit in the whole migration (Faz A Faz 4, index.js/progress.js last)." },
  "gamification.max_xp_events_per_call": { "type": "int", "default": 10, "min": 1, "max": 100, "doc": "server", "group": "gamification_server", "label": "syncProgress çağrısı başına maksimum XP olayı", "sensitive": false, "note": "MAX_XP_EVENTS_PER_CALL, progress.js:116." },
  "gamification.achievement_points": { "type": "map<string,int>", "default": { "firstMealLogged": 10, "firstPhotoLog": 15, "firstPost": 10, "firstCook": 20, "streak7": 30, "streak30": 75, "streak100": 200, "tierActive": 20, "tierContributor": 50, "tierExpert": 100, "tierLegend": 250, "level50": 300, "groupTop3": 40, "groupStreak4": 120, "gymRegular": 60 }, "doc": "server", "group": "gamification_server", "label": "Başarım puanları", "sensitive": true, "note": "ACHIEVEMENT_POINTS, progress.js:127-143 — 15 entries, exact keys/values verified directly. Its own comment: 'Mirrors lib/core/models/achievement_model.dart's kAchievementCatalog points column ... Cloud Functions can't import Flutter's lib/, so this is a deliberate, small, hand-kept-in-sync duplicate.' This migration is the mechanism that finally ends that hand-sync: the Dart side should read the generated defaults from this same schema entry (Faz A Faz 1 codegen), not its own independently-declared catalog." },
  "gamification.gym_regular_checkin_threshold": { "type": "int", "default": 15, "min": 1, "max": 100, "doc": "server", "group": "gamification_server", "label": "'Düzenli salon üyesi' check-in eşiği", "sensitive": false, "note": "GYM_REGULAR_CHECKIN_THRESHOLD, progress.js:152 — gates the gymRegular achievement." },
  "gamification.group_streak_achievement_threshold": { "type": "int", "default": 4, "min": 1, "max": 52, "doc": "server", "group": "gamification_server", "label": "Grup streak başarım eşiği (hafta)", "sensitive": false, "note": "GROUP_STREAK_ACHIEVEMENT_THRESHOLD, progress.js:160 — gates groupStreak4." },
  "gamification.tier_level_floor": { "type": "map<string,int>", "default": { "newcomer": 1, "active": 5, "contributor": 10, "expert": 20, "legend": 35 }, "doc": "server", "group": "gamification_server", "label": "İtibar kademesi seviye eşikleri", "sensitive": true, "note": "TIER_LEVEL_FLOOR, progress.js:207. Cross-field validation (A5): values must stay strictly ascending in TIER_ORDER=['newcomer','active','contributor','expert','legend'] order — a non-ascending edit would silently break tier derivation." },
  "gamification.local_utc_offset_hours": { "type": "int", "default": 3, "min": -12, "max": 14, "doc": "server", "group": "gamification_server", "label": "Yerel saat dilimi ofseti (UTC+)", "sensitive": false, "note": "LOCAL_UTC_OFFSET_HOURS, DELIBERATELY DUPLICATED (per its own comments) in THREE files: progress.js:165, presence.js:295 (as part of QUIET_HOURS calc), engagement_credit_logic.js:270. All three currently hardcode 3 (Turkey, fixed UTC+3, no DST since 2016). This migration consolidates to ONE config value all three read — ending the 3-way hand-duplication, unlike the deliberate WEEKLY_*_PER_RUN caps which stay separate (see gamification server-cap entries below)." },

  "moderation.post_min_text_length": { "type": "int", "default": 20, "min": 0, "max": 500, "doc": "server", "group": "moderation", "label": "Gönderi minimum metin uzunluğu", "sensitive": false, "note": "POST_MIN_TEXT_LENGTH, engagement_credit_logic.js:74. Its own comment: 'a deliberate, documented, easily-retuned choice — not derived from any data, since none exists yet for a pre-launch app' — this migration is exactly what that comment anticipated." },
  "moderation.comment_min_text_length": { "type": "int", "default": 10, "min": 0, "max": 500, "doc": "server", "group": "moderation", "label": "Yorum minimum metin uzunluğu", "sensitive": false, "note": "COMMENT_MIN_TEXT_LENGTH, engagement_credit_logic.js:75." },
  "moderation.message_min_text_length": { "type": "int", "default": 10, "min": 0, "max": 500, "doc": "server", "group": "moderation", "label": "Mesaj minimum metin uzunluğu", "sensitive": false, "note": "MESSAGE_MIN_TEXT_LENGTH, engagement_credit_logic.js:76." },
  "moderation.duplicate_similarity_threshold": { "type": "double", "default": 0.85, "min": 0.5, "max": 1.0, "doc": "server", "group": "moderation", "label": "Yinelenen içerik benzerlik eşiği", "sensitive": true, "note": "DUPLICATE_SIMILARITY_THRESHOLD, engagement_credit_logic.js:132 (Jaccard similarity, isNearDuplicateText). Sensitive: lowering this weakens anti-spam detection." },
  "moderation.duplicate_recent_window": { "type": "int", "default": 20, "min": 1, "max": 200, "doc": "server", "group": "moderation", "label": "Yinelenen içerik karşılaştırma penceresi", "sensitive": false, "note": "DUPLICATE_RECENT_WINDOW, engagement_credit_logic.js:135." },
  "moderation.reciprocity_min_pair_sample": { "type": "int", "default": 4, "min": 1, "max": 100, "doc": "server", "group": "moderation", "label": "Karşılıklılık eşiği — minimum örnek", "sensitive": true, "note": "RECIPROCITY_MIN_PAIR_SAMPLE, engagement_credit_logic.js:155." },
  "moderation.reciprocity_ratio_threshold": { "type": "double", "default": 0.5, "min": 0.0, "max": 1.0, "doc": "server", "group": "moderation", "label": "Karşılıklılık oran eşiği", "sensitive": true, "note": "RECIPROCITY_RATIO_THRESHOLD, engagement_credit_logic.js:156." },
  "moderation.reciprocity_downweight": { "type": "double", "default": 0.2, "min": 0.0, "max": 1.0, "doc": "server", "group": "moderation", "label": "Karşılıklılık ağırlık cezası", "sensitive": true, "note": "RECIPROCITY_DOWNWEIGHT, engagement_credit_logic.js:157." },
  "moderation.concentration_window": { "type": "int", "default": 20, "min": 1, "max": 200, "doc": "server", "group": "moderation", "label": "Yoğunlaşma tespiti — pencere", "sensitive": false, "note": "CONCENTRATION_WINDOW, engagement_credit_logic.js:177." },
  "moderation.concentration_distinct_max": { "type": "int", "default": 3, "min": 1, "max": 50, "doc": "server", "group": "moderation", "label": "Yoğunlaşma tespiti — maksimum farklı kaynak", "sensitive": true, "note": "CONCENTRATION_DISTINCT_MAX, engagement_credit_logic.js:178." },
  "moderation.concentration_downweight": { "type": "double", "default": 0.2, "min": 0.0, "max": 1.0, "doc": "server", "group": "moderation", "label": "Yoğunlaşma ağırlık cezası", "sensitive": true, "note": "CONCENTRATION_DOWNWEIGHT, engagement_credit_logic.js:179." },
  "moderation.min_account_age_ms": { "type": "int", "default": 259200000, "min": 0, "max": 2592000000, "doc": "server", "group": "moderation", "label": "Minimum hesap yaşı (ms)", "sensitive": true, "note": "MIN_ACCOUNT_AGE_MS = 3*24*60*60*1000, engagement_credit_logic.js:213 (verified: exactly 259200000). 'hesap yaşı >= 3 gün ve >= 1 doğrulanmış e-posta' eligibility gate. Max bound = 30 days, a sanity ceiling." },
  "moderation.auto_restrict_flag_threshold": { "type": "int", "default": 5, "min": 1, "max": 50, "doc": "server", "group": "moderation", "label": "Otomatik kısıtlama bayrak eşiği", "sensitive": true, "note": "AUTO_RESTRICT_FLAG_THRESHOLD, engagement_credit_logic.js:230 — min:1 enforced because 0 would disable the shadow-restriction defense entirely." },
  "moderation.report_rate_limit": { "type": "object", "default": { "windowMs": 600000, "max": 8, "lockMs": 900000 }, "doc": "server", "group": "moderation", "label": "Rapor gönderme hız sınırı", "sensitive": true, "note": "REPORT_RATE_WINDOW_MS/REPORT_RATE_MAX_IN_WINDOW/REPORT_LOCK_MS, moderation.js:45-47 (10min/8/15min)." },
  "moderation.action_rate_limit": { "type": "object", "default": { "windowMs": 600000, "max": 15, "lockMs": 900000 }, "doc": "server", "group": "moderation", "label": "Moderasyon aksiyonu hız sınırı", "sensitive": true, "note": "MOD_RATE_WINDOW_MS/MOD_RATE_MAX_IN_WINDOW/MOD_LOCK_MS, moderation.js:52-54 (10min/15/15min)." },
  "moderation.appeal_rate_limit": { "type": "object", "default": { "windowMs": 3600000, "max": 5, "lockMs": 3600000 }, "doc": "server", "group": "moderation", "label": "İtiraz gönderme hız sınırı", "sensitive": true, "note": "APPEAL_RATE_WINDOW_MS/APPEAL_RATE_MAX_IN_WINDOW/APPEAL_LOCK_MS, moderation.js:59-61 (1h/5/1h)." },
  "moderation.post_rate_limit": { "type": "object", "default": { "windowMs": 600000, "max": 20, "lockMs": 900000 }, "doc": "server", "group": "moderation", "label": "Gönderi oluşturma hız sınırı", "sensitive": true, "note": "SEC-12 — NEW, no prior hardcoded constant to migrate (this rate limit never existed before). functions/ugc_rate_limit.js. Shared 'post' budget across both posts/{postId} (top-level) and gyms/{gymId}/posts/{postId} (gym-scoped) — one spam budget per user, not one per collection. 10min/20/15min: generous for a real user posting a handful of times an hour, tight against a scripted flood." },
  "moderation.comment_rate_limit": { "type": "object", "default": { "windowMs": 600000, "max": 30, "lockMs": 900000 }, "doc": "server", "group": "moderation", "label": "Yorum oluşturma hız sınırı", "sensitive": true, "note": "SEC-12 — NEW. functions/ugc_rate_limit.js. Shared 'comment' budget across top-level posts/{postId}/comments and gyms/{gymId}/posts/{postId}/comments. Higher ceiling than post_rate_limit — commenting is naturally higher-frequency than posting in normal conversational use." },
  "moderation.message_rate_limit": { "type": "object", "default": { "windowMs": 60000, "max": 30, "lockMs": 300000 }, "doc": "server", "group": "moderation", "label": "Sohbet mesajı hız sınırı", "sensitive": true, "note": "SEC-12 — NEW. functions/ugc_rate_limit.js, chats/{chatId}/messages. Short 1-minute window sized for real chat pace (a fast back-and-forth conversation) rather than the 10-minute windows used elsewhere; 5min lock, shorter than report/appeal locks since a legitimate chat burst is more likely than a legitimate report burst." },
  "moderation.group_create_rate_limit": { "type": "object", "default": { "windowMs": 3600000, "max": 5, "lockMs": 3600000 }, "doc": "server", "group": "moderation", "label": "Grup oluşturma hız sınırı", "sensitive": true, "note": "SEC-12 — NEW. functions/ugc_rate_limit.js, community_groups/{groupId} create. Groups are created rarely in normal use (a handful ever, not per session) — 1h/5/1h is already generous for genuine use." },
  "moderation.follow_rate_limit": { "type": "object", "default": { "windowMs": 600000, "max": 50, "lockMs": 900000 }, "doc": "server", "group": "moderation", "label": "Takip etme hız sınırı", "sensitive": true, "note": "SEC-12 — NEW. functions/ugc_rate_limit.js, users/{uid}/following create. Higher ceiling — legitimately bursty (following many suggested accounts after browsing) is a real, common pattern, unlike posting/commenting bursts." },
  "moderation.reaction_rate_limit": { "type": "object", "default": { "windowMs": 300000, "max": 100, "lockMs": 600000 }, "doc": "server", "group": "moderation", "label": "Beğeni/tepki hız sınırı", "sensitive": true, "note": "SEC-12 — NEW. functions/ugc_rate_limit.js. Shared 'reaction' budget across post reactions, post likes, and comment likes (posts/{postId}/reactions, posts/{postId}/likes, posts/{postId}/comments/{commentId}/likes) — the highest-frequency casual UGC action (scrolling and reacting), so the highest ceiling of the six new limits." },
  "moderation.checkin_rate_limit": { "type": "object", "default": { "windowMs": 3600000, "max": 6, "lockMs": 3600000 }, "doc": "server", "group": "moderation", "label": "Check-in hız sınırı", "sensitive": true, "note": "SEC-08 — NEW. functions/gym.js, shared across validateGymCheckin (QR) and the new validateGymGpsCheckin (GPS/manual) — one spam budget per user across BOTH check-in methods, since both feed the same Gym Wars/leaderboard checkins collection. 1h/6/1h: a real gym visit is once, maybe twice a day (e.g. a workout + a class); 6/hour comfortably covers even an unusually active day while blocking a scripted flood." },

  "engagement.credit_table": { "type": "map<string,object>", "default": { "post_reactions": { "credit": 1, "dailyCap": 2, "threshold": 10 }, "comment_likes": { "credit": 1, "dailyCap": 1, "threshold": 5 }, "template_used": { "credit": 1, "dailyCap": 3 }, "weekly_group_top3": { "credit": 5, "weeklyCap": 1 } }, "doc": "server", "group": "gamification_server", "label": "AI kredi kazanım tablosu", "sensitive": true, "note": "CREDIT_TABLE, engagement_credit_logic.js:243-248. IMPORTANT: `threshold` (distinct-account count) is NEVER doubled by the premium 2x multiplier — only `credit` and cap values are (creditAndCapForPremium, :255-262). If this map's structure ever changes, that asymmetry must be preserved in the multiplier logic, not encoded as a separate config value." },
  "engagement.weekly_min_active_members": { "type": "int", "default": 3, "min": 2, "max": 50, "doc": "server", "group": "gamification_server", "label": "Haftalık grup katkı — minimum aktif üye", "sensitive": true, "note": "WEEKLY_MIN_ACTIVE_MEMBERS, engagement_credit.js:530. NOT a k-anonymity setting for gym analytics (see privacy.k_anonymity_threshold's note) — this gates whether a weekly group-contribution leaderboard reveals at all, an unrelated feature." },
  "engagement.weekly_contrib_groups_per_run": { "type": "int", "default": 300, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Haftalık katkı — çalıştırma başına grup sayısı", "sensitive": false, "note": "WEEKLY_CONTRIB_GROUPS_PER_RUN, engagement_credit.js:531. Operational batch-size cap, not a business value — wrong-in-either-direction risk is wasted compute (too high) or slow coverage (too low), not abuse or revenue." },
  "engagement.weekly_top_n": { "type": "int", "default": 3, "min": 1, "max": 20, "doc": "server", "group": "gamification_server", "label": "Haftalık kazanan sayısı", "sensitive": false, "note": "WEEKLY_TOP_N, engagement_credit.js:532." },
  "engagement.weekly_candidate_buffer": { "type": "int", "default": 10, "min": 1, "max": 100, "doc": "server", "group": "ops_caps", "label": "Haftalık aday tampon boyutu", "sensitive": false, "note": "WEEKLY_CANDIDATE_BUFFER, engagement_credit.js:535 — pulled as a ranked buffer so ineligible top-scorers can be skipped without a second query. Currently numerically equal to engagement.contrib_leaderboard_top_n (both 10) but they are DELIBERATELY SEPARATE entries: one is an internal query-efficiency buffer, the other is a user-visible display count, and code comments describe them as 'mirrors' of each other rather than a shared reference — consolidating them would risk silently coupling an internal performance knob to a user-facing display setting. Do not merge without a code change that actually shares them." },
  "engagement.contrib_leaderboard_groups_per_run": { "type": "int", "default": 300, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Canlı katkı panosu — çalıştırma başına grup sayısı", "sensitive": false, "note": "CONTRIB_LEADERBOARD_GROUPS_PER_RUN, engagement_credit.js:820 — 'mirrors WEEKLY_CONTRIB_GROUPS_PER_RUN' per its own comment, kept as a separate entry for the same reason as engagement.weekly_candidate_buffer." },
  "engagement.contrib_leaderboard_top_n": { "type": "int", "default": 10, "min": 1, "max": 50, "doc": "server", "group": "gamification_server", "label": "Canlı katkı panosu — gösterilen üye sayısı", "sensitive": false, "note": "CONTRIB_LEADERBOARD_TOP_N, engagement_credit.js:821 — this is the LIVE 'who's winning right now' display (distinct from engagement.weekly_top_n, which is the after-the-week-closes credit-awarding count)." },

  "presence.timestamp_skew_ms": { "type": "int", "default": 300000, "min": 10000, "max": 3600000, "doc": "server", "group": "presence", "label": "İzin verilen saat sapması (ms)", "sensitive": true, "note": "TIMESTAMP_SKEW_MS = 5*60*1000, presence.js:36 (verified 300000) — a client clock further off than this is rejected outright." },
  "presence.rate_limit_reentry_ms": { "type": "int", "default": 600000, "min": 60000, "max": 3600000, "doc": "server", "group": "presence", "label": "Yeniden giriş hız sınırı (ms)", "sensitive": false, "note": "RATE_LIMIT_REENTRY_MS = 10*60*1000, presence.js:39 (verified 600000) — GPS-flicker re-trigger guard." },
  "presence.presence_ttl_ms": { "type": "int", "default": 14400000, "min": 600000, "max": 86400000, "doc": "server", "group": "presence", "label": "Oturum geçerlilik süresi (ms)", "sensitive": false, "note": "PRESENCE_TTL_MS = 4*60*60*1000, presence.js:44 (verified 14400000) — absolute safety-net expiry swept by closeStalePresenceSessions." },
  "presence.stale_sweep_limit": { "type": "int", "default": 200, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Eski oturum temizleme limiti", "sensitive": false, "note": "STALE_SWEEP_LIMIT, presence.js:45." },
  "presence.max_friends_fanout": { "type": "int", "default": 300, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Arkadaş bildirimi fan-out limiti", "sensitive": false, "note": "MAX_FRIENDS_FANOUT, presence.js:289 — defensive cap independent of any real friends-list size cap." },
  "presence.notify_log_ttl_days": { "type": "int", "default": 2, "min": 1, "max": 30, "doc": "server", "group": "presence", "label": "Bildirim log TTL (gün)", "sensitive": false, "note": "NOTIFY_LOG_TTL_DAYS, presence.js:291 — one notification per (receiver, friend, gym) per calendar day dedup window." },
  "presence.quiet_hours_start": { "type": "int", "default": 7, "min": 0, "max": 23, "doc": "server", "group": "presence", "label": "Sessiz saat başlangıcı (yerel saat)", "sensitive": false, "note": "QUIET_HOURS_START, presence.js:296. Cross-field (A5): should be < presence.quiet_hours_end under normal same-day interpretation." },
  "presence.quiet_hours_end": { "type": "int", "default": 23, "min": 0, "max": 23, "doc": "server", "group": "presence", "label": "Sessiz saat bitişi (yerel saat)", "sensitive": false, "note": "QUIET_HOURS_END, presence.js:297." },

  "groups.activity_window_hours": { "type": "int", "default": 24, "min": 1, "max": 168, "doc": "server", "group": "ops_caps", "label": "Aktivite skoru penceresi (saat)", "sensitive": false, "note": "ACTIVITY_WINDOW_HOURS, groups.js:111." },
  "groups.activity_half_life_hours": { "type": "double", "default": 6, "min": 1, "max": 168, "doc": "server", "group": "ops_caps", "label": "Aktivite skoru yarı ömrü (saat)", "sensitive": false, "note": "ACTIVITY_HALF_LIFE_HOURS, groups.js:112 — decay weighting for the 'hot right now' discovery carousel score." },
  "groups.activity_groups_per_run": { "type": "int", "default": 200, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Aktivite skoru — çalıştırma başına grup sayısı", "sensitive": false, "note": "ACTIVITY_GROUPS_PER_RUN, groups.js:119 — only is_public groups are scored." },
  "groups.activity_signal_limit": { "type": "int", "default": 300, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Aktivite skoru — sinyal başına tarama limiti", "sensitive": false, "note": "ACTIVITY_SIGNAL_LIMIT, groups.js:123." },
  "groups.activity_comments_scan_limit": { "type": "int", "default": 500, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Aktivite skoru — yorum tarama limiti", "sensitive": false, "note": "ACTIVITY_COMMENTS_SCAN_LIMIT, groups.js:127." },

  "summaries.gen_rate_window_ms": { "type": "int", "default": 86400000, "min": 3600000, "max": 604800000, "doc": "server", "group": "ops_caps", "label": "Özet üretim hız penceresi (ms)", "sensitive": false, "note": "GEN_RATE_WINDOW_MS = 24*60*60*1000, summaries.js:56 (verified 86400000) — per (caller, member) pair." },
  "summaries.gen_rate_max_in_window": { "type": "int", "default": 1, "min": 1, "max": 10, "doc": "server", "group": "ops_caps", "label": "Pencere başına maksimum özet", "sensitive": false, "note": "GEN_RATE_MAX_IN_WINDOW, summaries.js:57." },
  "summaries.ttl_ms": { "type": "int", "default": 604800000, "min": 3600000, "max": 2592000000, "doc": "server", "group": "ops_caps", "label": "Özet TTL (ms)", "sensitive": false, "note": "SUMMARY_TTL_MS = 7*24*60*60*1000, summaries.js:62 (verified 604800000)." },
  "summaries.stale_sweep_limit": { "type": "int", "default": 300, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Eski özet temizleme limiti", "sensitive": false, "note": "STALE_SWEEP_LIMIT, summaries.js:63." },
  "summaries.max_scope_members_scan": { "type": "int", "default": 500, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Kapsam üyesi tarama limiti", "sensitive": false, "note": "MAX_SCOPE_MEMBERS_SCAN, summaries.js:69 — getConsentingMemberUids candidate scan cap, matches gym_analytics_service.dart's memberCap=500 convention." },

  "templates.offer_ttl_days": { "type": "int", "default": 14, "min": 1, "max": 90, "doc": "server", "group": "economy", "label": "Plan teklifi geçerlilik süresi (gün)", "sensitive": false, "note": "OFFER_TTL_DAYS, templates.js:34 — '14 günde otomatik expired', swept by expirePlanOffers." },
  "templates.max_recipients_per_call": { "type": "int", "default": 100, "min": 1, "max": 1000, "doc": "server", "group": "ops_caps", "label": "Çağrı başına maksimum alıcı", "sensitive": false, "note": "MAX_RECIPIENTS_PER_CALL, templates.js:32." },
  "templates.max_message_length": { "type": "int", "default": 500, "min": 10, "max": 5000, "doc": "server", "group": "ops_caps", "label": "Maksimum mesaj uzunluğu", "sensitive": false, "note": "MAX_MESSAGE_LENGTH, templates.js:33." },

  "media.vision_daily_cap": { "type": "int", "default": 1000, "min": 10, "max": 100000, "doc": "server", "group": "ai_server", "label": "Günlük Vision taraması limiti", "sensitive": true, "note": "VISION_DAILY_CAP, functions/media.js:42 — currently env-driven (process.env.VISION_DAILY_CAP || '1000'), migrating to config makes it admin-editable without an env-var redeploy. Its own comment: 'at ~$1.5/1000 images, a cap of 1000/day bounds worst-case spend to roughly $1.5/day' — sensitive because raising it directly raises worst-case Vision spend." },

  "cost.pricing": {
    "type": "object",
    "default": {
      "firestoreReadPer100k": 0.06, "firestoreWritePer100k": 0.18, "firestoreDeletePer100k": 0.02,
      "firestoreStoragePerGiBMonth": 0.18, "firestoreFreeStorageGiB": 1.0,
      "firestoreFreeReadsPerDay": 50000, "firestoreFreeWritesPerDay": 20000,
      "storagePerGBMonth": 0.026, "storageDownloadPerGB": 0.12, "storageFreeGB": 5.0,
      "functionsInvocationPerMillion": 0.40, "functionsFreeInvocationsPerMonth": 2000000,
      "visionPer1000": 1.50, "visionFreePerMonth": 1000,
      "openRouterCostPerCallUsd": 0.0, "openRouterCostPerVisionCallUsd": 0.0,
      "premiumMonthlyPrice": 4.99, "premiumYearlyPricePerMonth": 3.33, "aiCreditsPackPrice": 1.99,
      "storeCutFraction": 0.15
    },
    "doc": "server", "group": "economy", "label": "Maliyet/gelir varsayımları (admin cost dashboard)", "sensitive": true,
    "note": "Consolidates FirebasePricing/OpenRouterPricing/RevenueAssumptions, lib/core/models/cost_analytics_model.dart:12-58, verified directly against source. These are ESTIMATES (Firebase does not expose real billing inside the app) feeding AdminCostAnalyticsScreen/CostAnalyticsService.estimate()/simulateAt(). Per the metrics-layer plan (Faz D), the real fix is a Cloud Billing→BigQuery export that validates/auto-tunes these assumptions rather than trusting hand-entered numbers indefinitely — this config entry makes the CURRENT hand-entered numbers admin-editable as an interim step, not a replacement for that eventual real-billing comparison. RevenueAssumptions is USD; economy.gym_commission_try/referral_commission_try are TRY — any blended margin view must carry an explicit FX rate + as-of date, never silently mix currencies (see Faz D §7.1)." }
}
